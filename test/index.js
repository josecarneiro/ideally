'use strict';

describe('Application Tests', () => {
  /* BEFORE AND AFTER TEST HOOKS */
  before((done) => {
    done();
  });

  after((done) => {
    done();
  });

  /* REQUIRE TESTS */
  require('./id');
});
